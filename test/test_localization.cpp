#include <iostream>

int main(int argc, char** argv)
{
    // Initialize simulator world

    // Initialize graph with objects from sim world

    // Give initial guess for robot pose (add robot to graph)

    // while true, associate, localize, update,

    //
    std::cout << "Hello world" << std::endl;
}
